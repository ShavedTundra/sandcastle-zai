---
"@ai-hero/sandcastle": patch
---

Document Z.ai model ID collision with built-in `opencode` provider. Users must use fully-qualified `zai/` prefix (e.g. `pi("zai/glm-5.1")`) to avoid ambiguity. README updated with a new "Z.ai model IDs" section; CHANGELOG updated.
