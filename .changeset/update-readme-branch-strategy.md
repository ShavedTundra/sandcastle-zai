---
"@ai-hero/sandcastle": patch
---

Update README documentation to use `branchStrategy` terminology throughout. Replace all references to the removed `worktree` option on `RunOptions` with `branchStrategy` on the sandbox provider. Update code examples, the "How it works" section, and option tables.
